# MacOCR Backend Dependencies
#
# Install for development:
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install torch torchvision
#   pip install --no-build-isolation "git+https://github.com/facebookresearch/detectron2.git"
#   pip install -r requirements.txt

fastapi>=0.136,<1.0
uvicorn[standard]>=0.49,<1.0
python-multipart>=0.0.32
aiofiles>=25.1,<26
aiohttp>=3.14,<4.0
img2pdf>=0.6,<1.0

# OCR Engine
magic-pdf==1.3.12

# transformers pinned for LayoutLMv3 compatibility (find_pruneable_heads_and_indices removed in 4.50+)
transformers==4.49.0

# magic-pdf deps
doclayout_yolo>=0.0.2b1
rapid-table>=1.0.5
modelscope>=1.37
timm>=1.0
ultralytics>=8.4
ftfy>=6.3
shapely>=2.1
pyclipper>=1.4

# Optional: LLM-assisted post-processing
openai>=1.109
